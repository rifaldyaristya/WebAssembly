<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Arithmatic Page</title>
</head>
<body>
  <h1>Welcome to Arithmatic Page</h1>
  <p>
    <% var matrix = [[1,2],[3,4]]; %>
    <% matrix.forEach(function(array){ %>
      <div>
        <% array.forEach(function(element){ %>
          <%= element %>
        <% }) %>
      </div>
    <% }); %>
    X
    <% var matrix2 = [[1,2],[3,4]]; %>
    <% matrix2.forEach(function(array){ %>
      <div>
        <% array.forEach(function(element){ %>
          <%= element %>
        <% }) %>
      </div>
    <% }); %>
    The result is:
    <% var mtrxRes = [] %>
    <% matrix.forEach(function(array,i){ %>
      <div>
        <% array.forEach(function(element){ %>
          <%= element %>
        <% }) %>
      </div>
    <% }); %>
  </p>
</body>
</html>